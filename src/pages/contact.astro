---
import Layout from '../layouts/Layout.astro';

/*Do not add labels to this!*/
const links = [
  { href: "mailto:oscar.djuric@gmail.com", value: "oscar.djuric@gmail.com" },
  { href: "https://www.linkedin.com/in/oscar-djuric/", value: "LinkedIn" },
  { href: "https://github.com/bibi848", value: "GitHub" },
  { href: "/CV_Oscar_Djuric.pdf", value: "Download CV", download: "CV_Oscar_Djuric.pdf" }
];
---

<Layout title="Contact | Oscar Djuric">
  <h1 class="page-title">Contact</h1>
  <p class="intro">
    Let's talk.
  </p>

  <section>
    <ul class="contact-list">
      {links.map((link) => (
        <li>
          <span class="label">{link.label}</span>
          <a 
            href={link.href} 
            target="_blank" 
            rel="noopener noreferrer"
            download={link.download || undefined}
          >
            {link.value}
          </a>
        </li>
      ))}
    </ul>
  </section>
</Layout>

<style>
  .intro {
    color: var(--color-white);
    max-width: 36rem;
  }

  .contact-list {
    list-style: none;
    padding: 0;
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .contact-list li {
    position: relative;
    overflow: visible;
    background-color: #151515;
    border-left: 3px solid var(--color-fuchsia-soft);
    padding: 1rem 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  /* Dither fades outward from the straight left edge only. */
  .contact-list li::after {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    left: -16px;
    width: 16px;
    pointer-events: none;
    opacity: 0.8;
    background-image: radial-gradient(circle, var(--color-fuchsia-soft) 0 0.8px, transparent 1px);
    background-size: 6px 6px;
    mask-image: linear-gradient(to right, transparent 0%, black 75%, black 100%);
    -webkit-mask-image: linear-gradient(to right, transparent 0%, black 75%, black 100%);
  }

  .label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #888;
  }

  .contact-list a {
    font-size: 1.05rem;
    word-break: break-word;
  }
</style>